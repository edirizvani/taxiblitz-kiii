# DEMO value only (same placeholder as .env.example).
# Must match the password in web-secret's connection string.
apiVersion: v1
kind: Secret
metadata:
  name: mssql-secret
  labels:
    app.kubernetes.io/name: mssql
type: Opaque
stringData:
  MSSQL_SA_PASSWORD: "ChangeMe_Strong!Passw0rd"
