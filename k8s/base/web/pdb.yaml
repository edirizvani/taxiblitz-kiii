# Voluntary disruptions (node drain, cluster upgrade) must leave at least one web pod running.
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: taxiblitz-web
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: taxiblitz-web
